/** @type {import('next').NextConfig} */
const nextConfig = {
    trailingSlash: true,
    output: "export",
};

module.exports = {
    images: {
        domains: ['i.dummyjson.com'],
    },
}, nextConfig

